<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .signup-container {
            max-width: 800px;
            height: 500px;
            margin: auto;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            background: #007bff26;
        }
        .left-section {
           
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 40px;

        }
        .right-section {
            background: #007bff;
            color: white;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 40px;
            border-top-left-radius: 100px;
            border-bottom-left-radius: 100px;
        }
    </style>
</head>
<body>
    <div class="container d-flex align-items-center vh-100">
        <div class="row signup-container w-100">
            <!-- Left Section (Now contains the Signup Form) -->
            <div class="col-md-6 left-section text-center">
                <h2 class="text-center">Sign Up</h2>
                <form>
                    <div class="mb-3">
                        <input type="text" class="form-control" placeholder="USN">
                    </div>
                    <div class="mb-3">
                        <input type="email" class="form-control" placeholder="College Email">
                    </div>
                    <div class="mb-3">
                        <input type="password" class="form-control" placeholder="Password">
                    </div>
                    <div class="mb-3">
                        <input type="password" class="form-control" placeholder="Confirm Password">
                    </div>
                    <button type="submit" class="btn btn-primary w-100 mt-3">Sign Up</button>
                </form>
            </div>
            
            <!-- Right Section (Now contains the Welcome Message) -->
            <div class="col-md-6 right-section text-center">
                <h2>Welcome!</h2>
                <p>Already have an account?</p>
                <button class="btn btn-light text-primary">Login</button>
            </div>
        </div>
    </div>
</body>
</html>
